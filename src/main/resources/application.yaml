spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai-springboot?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456
#服务器配置
server:
  port: 8090
#日志配置
logging:
  level:
    root: INFO
